// Primitive

// 7 Datatypes in JS : string, number, boolean, null, undefined, null, symbol. bigint

// refrence type (non-primitive) : array, objects, functions

// JS is dynamically types language 

const id = Symbol('123')
const anotherId = Symbol('123')

console.log(id === anotherId);